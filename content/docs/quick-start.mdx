import { T } from "@/components/docs/mdx-i18n-wrapper";

# <T translationKey="docs.quickStart.title" />

<T translationKey="docs.quickStart.description">
  Run the following command on your VPS to start benchmarking:
</T>

```bash
bash <(curl -fsSL https://tocdovps.dev/install)
```

<T translationKey="docs.quickStart.note">
  The script will automatically download and run benchmark tests. You'll be
  prompted for language and sharing preferences.
</T>
